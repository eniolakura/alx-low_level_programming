#include <stdio.h>
/**
 * main - prints natural numbers that are below 1024 and are multiples of 3 or 5
 *
 * Return: Always 0.
 */
int main(void)
{
	int a, sum;

	for (a = 1; a < 1024; a++)
	{
		if ((a % 3) == 0 || (a % 5) == 0)
			sum += a;
	}
	printf("%d\n", sum);
	return (0);
}
